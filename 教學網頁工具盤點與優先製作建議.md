# 數學教學網頁工具盤點、缺口與優先強化建議

更新日期：2026-08-20
盤點範圍：本專案根目錄目前的首頁、所有 HTML 教學頁、首頁章節目錄與 PDF 匯出相關檔案。

## 0. 先看結論

這個網站已經不再是「只有幾個數學小遊戲」的階段，而是已有 **53 個內容頁** 的國中到高三教學工具庫。強項很明確：圖像化、可操作的幾何與函數頁很多；不少頁面也已有「概念 → 例題 → 常見錯誤 → 自我檢查」的教學骨架。

下一步最重要的，不是平均地再加很多頁，而是先完成三件事：

1. **讓所有已做好的頁面找得到、分得對、能順利回到首頁。** 目前 53 個內容頁中有 51 個能由首頁進入；另有 2 個已編入章節目錄、卻尚未放入首頁卡片。
2. **補國中最關鍵的代數主幹。** 最優先是「一元一次方程式」與「一次函數的生活建模」；接著要把新加入的因式分解與一元二次方程式，銜接成國三可循序學習的路徑，再補上二次函數。它們正好能把既有的運算、未知數、不等式、直線與函數工具串起來。
3. **把單頁的漂亮互動，提升為可追蹤的學習流程。** 現有許多頁有自我檢查，但練習形式、錯誤回饋、完成紀錄與教師列印功能尚未統一。

---

## 1. 本次盤點的依據與可確認範圍

### 1.1 已確認的現況

| 項目 | 本次結果 | 解讀 |
|---|---:|---|
| 根目錄 HTML | 54 頁 | 1 個首頁 `index.html`，加上 53 個內容／工具頁。 |
| 首頁可進入的內容頁 | 51 頁 | 首頁的卡片連結都指向存在的檔案，沒有失效目標。 |
| 已登錄在 `page-chapter-catalog.js` 的內容頁 | 53 頁 | 53 頁皆有章節代碼與排序資料，沒有漏登或指向不存在檔案的紀錄。 |
| 首頁尚未可發現的頁面 | 2 頁 | `minesweeper-screenshot-solver.html`、`queen-solver.html` 已登錄目錄，但沒有首頁卡片。 |
| 內嵌 JavaScript 語法 | 54 頁皆通過 | 此為將每一段內嵌 script 個別解析的靜態檢查。 |
| 本機 HTTP 開啟 | 54 頁皆為 HTTP 200 | 以 `http://127.0.0.1:8789/` 檢查，不以 `file://` 判定。 |

### 1.2 本次尚未宣稱完成的驗收

本次可確認原始碼、首頁連結、章節目錄、HTTP 回應與內嵌 JavaScript 語法；但執行環境的瀏覽器控制連線未能建立，因此**沒有把「每一個按鈕皆已在真實瀏覽器逐一操作、每個 SVG／Canvas 都無裁切、手機版都無橫向溢位」列為已完成**。

因此本文件中的「已具備互動」「有自我檢查」是依頁面結構、控制元件與內容設計判斷；後續進入改版時，仍要逐頁做實際瀏覽器驗收。

---

## 2. 現有工具地圖：依首頁實際章節重整後的 51 個入口

首頁目前會由 `page-chapter-catalog.js` 在載入時，將原始卡片重組為 12 個章節入口。以下以**實際重組後的章節歸屬**盤點，而非只看 HTML 原本的靜態位置。

| 學段／章節 | 頁數 | 已有內容與主要頁面 | 教學定位 |
|---|---:|---|---|
| 國一上 `j1` | 4 | 重要數字與心算、運算金字塔、因數倍數、未知數／變數／常數 | 數感、代數語言與運算先備。 |
| 國一下 `j2` | 3 | 比例與正反比、一元一次不等式、3×3×3 三視圖 | 比例、範圍語言與空間表徵。 |
| 國二上 `j3` | 3 | 根號估計與直式開根號、畢氏數與特殊直角三角形、因式分解練習室 | 平方根、代數式結構與直角三角形的數形連結。 |
| 國二下 `j4` | 5 | 數列與級數、尺規作圖、圖形比例公式地圖、平行四邊形、四邊形翻牌 | 幾何作圖、面積比例與性質複習。 |
| 國三上 `j5` | 5 | 三角形內點面積比、平行四邊形比例、圓、三角形四心、三角形全等 | 相似／比例、圓與三角形推理。 |
| 國三下 `j6` | 1 | 機率實驗室：硬幣、骰子、抽球、猜拳 | 由樣本空間進入理論機率與實驗。 |
| 高一上 `s1` | 9 | 指數與科學記號、絕對值、直線方程式、四心座標、基本函數圖形、多項式除法、高中多項式、係數讀圖、一元二次方程式 | 數與式、解析幾何、多項式、方程式與函數圖像。 |
| 高一下 `s2` | 6 | 遞迴／等差等比、邏輯集合、排列組合、數據分析、三角比、三角形面積公式 | 數列、計數、資料分析與三角。 |
| 高二上 `s3` | 5 | 最大最小值、函數變換、指數對數公式與證明、平面向量內積、向量線性組合與七區 | 函數思維、證明與向量。 |
| 高二下 `s4` | 6 | 空間向量、三平面、空間直線與平面、條件機率與貝氏、矩陣變換、矩陣高次乘法 | 空間幾何、機率與矩陣。 |
| 高三 `s5` | 2 | 微積分學習地圖、微積分核心定理與進階微分 | 從概念地圖、定理證明走向進階技巧。 |
| 其他工具 | 2 | 迷宮、記憶翻牌 | 課堂暖身、專注力與轉場活動。 |

### 2.1 已編目但尚未放入首頁的 2 個工具

| 頁面 | 目前內容 | 建議處置 |
|---|---|---|
| `minesweeper-screenshot-solver.html` | 由截圖讀取踩地雷局部盤面，標出必為地雷與必安全格。 | 若定位為「邏輯推理／資訊活動」，在「其他工具」新增卡片並清楚標為跨域邏輯；若不是本網站的長期範圍，應從章節目錄與發行清單移出。 |
| `queen-solver.html` | 由 Queen 遊戲截圖校正棋盤並求解。 | 同上。若保留，需補「八皇后條件如何轉成數學限制」的教學段落，避免只像遊戲解題器。 |

---

## 3. 現有內容的教學優勢

### 3.1 已經做得好的方向

1. **先有圖像或操作，再給符號。**
   - 絕對值以數線距離與拖曳位置理解 \(\lvert x-y\rvert\)。
   - 直線方程式可從兩點、斜截式、一般式、點斜式、參數式互相轉換。
   - 函數係數頁讓斜率、截距、頂點、判別式與圖形同時變動。
   - 圓、三角形全等、三角形內點、平行四邊形比例、空間向量都以 SVG／Canvas 協助判讀。

2. **有不少頁面能直接拿去投影上課。**
   例如：`absolute-value-learning.html`、`circle-properties-angles.html`、`j6-3-probability-learning.html`、`line-from-points.html`、`triangle-congruence-learning.html`、`function-transformations-x-y-prime.html`。這些頁面同時有可操作區與文字引導，適合老師先帶一次、學生再自行操作。

3. **高中內容不只放公式，也有理由與選法。**
   最大最小值強調「條件 → 結構 → 界 → 可取性」；矩陣頁用反例拆除錯誤直覺；微積分頁從極限、切線與累積量建立脈絡。這些都是學生真正容易卡住的地方。

4. **錯誤觀念已被主動點出。**
   例如：絕對值的距離意義、圓的弦與弧、互斥與獨立、矩陣乘法不能交換、全等與相似／SSA 的區別。這比只列公式更有教學價值。

5. **題型練習已開始有分層概念。**
   因數倍數、根號、畢氏數、三角比、機率、尺規作圖等頁已見到基礎、進階、挑戰或自測的安排，可作為全站共同練習框架的樣板。

### 3.2 已有工具的型態分布

- 約 36 個內容頁有 MathJax／公式排版相關設計，適合呈現正式推導。
- 多數教學頁有按鈕、輸入欄、滑桿、SVG 或 Canvas；幾何、機率、函數頁的互動密度尤其高。
- 47 個頁面可由「練習、自測、測驗、答案檢查」等文字或互動結構辨識到自我檢查訊號；但這不表示 47 頁都已有同樣品質的逐步回饋。
- 35 個頁面包含老師、板書、課堂或教學建議等語句，表示網站已考慮教師使用情境。

---

## 4. 目前最需要加強的地方

### A. 首頁、章節與可發現性

| 優先 | 問題 | 已觀察到的證據 | 建議強化方式 |
|---|---|---|---|
| A1 | 有頁面存在卻找不到 | 53 個內容頁皆已編目，但 2 個頁面沒有首頁卡片。 | 先決定 Queen 與踩地雷是否屬正式工具；保留就補卡片、年級／用途標籤與教學目標。 |
| A1 | 首頁有兩套分類來源 | 原始 HTML 內仍保留較舊的學段面板與數量；執行 JavaScript 後才依章節目錄重組為 12 個章節。 | 將卡片資料、章節代碼、排序與首頁顯示逐步收斂成單一來源，或至少新增自動檢查：目錄頁數 = 首頁卡片頁數。 |
| A2 | 導覽不夠以「使用情境」找工具 | 首頁目前以章節為主，缺少「新課引導、課堂互動、觀念複習、題型練習、考前統整」入口。 | 增加用途標籤／篩選；例如「可投影操作」「有隨機題」「有圖像拖曳」「可列印」。 |
| A2 | 回到首頁的方式不一致 | 有 9 個內容頁在原始碼中找不到 `index.html` 連結字串；不代表一定無返回鍵，但需逐頁確認。 | 全部教學頁固定放置可見的「回工具首頁」與「本章下一頁」；手機版也要在頁首可用。 |

### B. 國中主幹概念仍有斷點

目前國中幾何與互動頁很豐富，但代數主線從「未知數」跳到「一元一次不等式」，再跳到高中的直線與函數，學生容易缺少中間的可理解階梯。

| 優先 | 缺少或不足的主題 | 為何應優先補 | 建議頁面與最低教學內容 |
|---|---|---|---|
| B1 | 一元一次方程式 | 是未知數、比例、應用題、不等式與一次函數的共同基礎；目前沒有一個完整的「等量 → 解方程 → 代回檢查」頁。 | `linear-equations-learning.html`：天平／等量圖、移項的理由、含括號與分數、文字題翻譯、代回檢查、錯誤診斷。 |
| B1 | 一次函數與線性關係 | 現有直線工具很完整，但偏高一解析幾何；國中生仍需要表格、情境、斜率與截距的基本建模。 | `linear-functions-learning.html`：車資／固定費＋用量情境，表格 → 點 → 圖 → \(y=mx+b\)，並分辨正負斜率、平行、交點。 |
| B1 | 因式分解、一元二次方程式、二次函數 | `factorization-practice.html` 與 `quadratic-formula-completing-square.html` 已補進來，但分屬 `j3-3` 與 `s1-3`；目前仍缺國三可順接的二次函數頁與明確跨頁路徑。 | 先在兩頁互相連結「因式分解 → 求根 → 配方／公式 → 根與圖形」，再做 `quadratic-functions-learning.html`，將式的結構、零點與拋物線連在一起。 |
| B2 | 平方根的四則運算與根式化簡 | 現有根號估計很有特色，但「\(\sqrt{a}\) 的運算規則、同類根式、分母有根號」尚未形成完整教學線。 | 補一個根式運算頁，先用面積與邊長直覺，再放入化簡、合併與有理化。 |
| B2 | 相似三角形的完整概念 | 現有全等、面積比、比例題都很強，但缺一頁專門處理對應角、對應邊、AA 相似、比例式與生活測量。 | `similar-triangles-learning.html`：先標對應順序，再列比例；放入影子量高、平行線截比與常見對錯配對。 |
| B2 | 統計基礎與資料判讀 | 已有高中的平均、標準差、相關與迴歸，但國中所需的平均數、中位數、眾數、四分位、圖表判讀與抽樣尚未獨立呈現。 | `junior-data-literacy.html`：同一組資料讓平均數／中位數不同，直方圖、圓餅圖、長條圖、誤導圖表與抽樣偏差。 |

### C. 高中內容需從「資料豐富」走向「可練會」

| 優先 | 目前頁面／群組 | 現有優點 | 仍需加強 |
|---|---|---|---|
| C1 | 微積分兩頁 | 概念地圖、定理脈絡、證明與進階方法兼具。 | 缺少從極限、微分、積分各自出發的短題組與方法辨識練習；學生知道地圖，不一定能做計算題。建議拆出「極限」「導數應用」「不定積分」「定積分面積」四個可作答的小工具。 |
| C1 | 多項式與函數 | 長除法、餘式定理、係數讀圖與最值已有高品質講解。 | `polynomial-division-learning.html`、`high-school-polynomials.html`、`maximum-minimum-methods.html`偏講義型；應加入每一步可輸入的過程題，以及「看題先選方法」的診斷題。 |
| C1 | 三角內容 | 三角比與三角形面積公式已具基礎。 | 尚缺三角函數圖形、和差倍半角／恆等式、正弦定理與餘弦定理的完整練習線。可先做最常用的「正餘弦定理 + 解三角形」頁。 |
| C2 | 向量與空間 | 平面內積、空間關係、三平面與方程式工具很完整；新加入的 `vector-linear-combination-regions.html` 已補上線性組合與係數位置。 | 接下來要在向量線性組合、內積與空間向量三頁互加「上一頁／下一頁」學習路徑，並補少量向量加減與倍數的基礎自測，避免學生跳頁。 |
| C2 | 複數與方程式 | 現有高一數與式頁集中於指數、絕對值、多項式。 | 若以完整高中數學工具庫為目標，仍需規畫複數、根與係數關係、方程式根的結構等單元。 |

### D. 單頁教學設計需要更一致

| 類型 | 代表頁面 | 評估 | 建議補強 |
|---|---|---|---|
| 操作很強，但題目閉環較弱 | `three-view-3x3x3.html`、`geometry-ratios-formula-guide.html` | 有很好的觀察材料，但學生操作後不一定知道自己是否真的會判讀。 | 增加隨機任務、作答欄、提示、解析與「下一題」。三視圖可讓學生依指定前／右／俯視圖堆出立體。 |
| 內容扎實，但偏閱讀式講義 | `maximum-minimum-methods.html`、`high-school-polynomials.html`、`exponent-logarithm-formula-proofs.html`、`triangle-area-formulas.html`、`calculus-proof-methods.html` | 適合老師備課與考前複習，但學生容易一路捲動而未檢查理解。 | 每 1 至 2 個觀念加入一題「選方法」或「補下一步」；錯誤回饋要說明條件遺漏、對象讀錯或公式誤用。 |
| 複習遊戲與正式概念分離 | `geometry-memory-game-v1.html`、`memory-game.html` | 適合暖身，但難以判斷是否真的理解。 | 在遊戲結束後顯示錯誤概念清單，並連到相對應的概念頁或補救題。 |
| 截圖解題工具 | `minesweeper-screenshot-solver.html`、`queen-solver.html` | 有推理潛力，但目前學科連結與首頁定位不足。 | 加入「限制條件、反證、排列、圖論／邏輯」的數學說明，或明確放入跨域邏輯工具區。 |

---

## 5. 全站共同缺口：比新增單頁更值得先做的能力

### 5.1 統一的三層練習與錯誤回饋

目前各頁的按鈕、題型、提示與答案呈現方式不同。建議日後的新頁與大改頁都至少使用同一個教學閉環：

1. **基礎：看懂概念。** 判斷、配對、讀圖，提示可以較直接。
2. **進階：自己列式。** 輸入一小步或完整答案，回饋要指出錯在符號、對應關係、範圍或計算。
3. **挑戰：情境建模或混合題。** 先判斷該用什麼概念，再計算並檢查單位／合理性。

錯誤回饋不要只有「答錯了」。至少分辨：

- 觀念錯：例如把互斥當獨立、把弦當弧。
- 表徵錯：例如圖上的端點空實心點看錯、對應邊配錯。
- 規則錯：例如不等式乘負數未反向。
- 計算錯：例如移項、通分、符號或四則順序錯。

### 5.2 學生學習紀錄與補救路徑

目前只有極少數頁面可在原始碼中看見進度或儲存訊號；大部分學生關掉頁面後，系統無法知道他會什麼、錯什麼、下一步該做什麼。

建議先不做複雜帳號系統，而是逐步增加瀏覽器本機紀錄：

- 每一頁記錄已完成的核心題、常錯類型與最後練習日期。
- 顯示「本頁已掌握／仍需複習」而非只有總分。
- 練習錯誤時提供下一頁連結，例如不等式錯在數線就回到絕對值／數線題；一次函數不熟就回到比例與直線作圖。

### 5.3 教師模式、列印與講義

網站已有整頁 PDF 匯出工具，能把單一網頁輸出成長頁 PDF；這是良好起點，但尚不能完全取代教師真正需要的教材。

下一步應補：

- 每頁 5 至 10 分鐘的投影流程：開場提問、操作指令、板書句、停下來問學生的問題。
- 可勾選的「學生講義版」：隱藏答案與過多說明，保留例題空格與練習區。
- 「教師答案版」：保留解析、常見錯誤與延伸追問。
- A4 分頁輸出，而非只以單一極長頁輸出；數學公式與 SVG 需在列印後再視覺檢查。

### 5.4 手機、可近用性與離線情境

許多頁面含大量 SVG／Canvas／MathJax，是強項也帶來風險。後續需用實際瀏覽器逐頁檢查：

- 390px 寬度下是否有橫向捲動、按鈕重疊、公式被截斷。
- 鍵盤操作能否進入控制項；按鈕是否有清楚文字與焦點狀態。
- 色彩之外是否仍能辨識正誤、線段、角度與圖例。
- 公式載入失敗時是否至少顯示可讀文字；有 MathJax 設計的頁面尤需檢查網路／快取狀況。

---

## 6. 建議的實作優先順序

### 第一階段：先讓現有資產可被可靠使用

1. **首頁與目錄一致性檢查**
   - 決定並處理 Queen、踩地雷兩頁的正式定位。
   - 增加「首頁卡片與章節目錄一致」的檢查。
   - 將所有頁面的回首頁與下一步學習路徑統一。

2. **首頁增加用途標籤**
   - 篩選：新課引導、可操作演示、題型自測、考前統整、暖身活動。
   - 顯示：適合年級、先備知識、建議上課時間、是否可列印。

3. **建立共同練習元件規格**
   - 基礎／進階／挑戰、提示、答案解析、重試、錯誤類型、完成狀態。
   - 先套用到 1 至 2 個代表頁，確認介面與教學語氣，再逐步擴充。

### 第二階段：補最重要的國中代數主線

1. `linear-equations-learning.html`：一元一次方程式。
2. `linear-functions-learning.html`：一次函數與生活情境。
3. 將 `factorization-practice.html` 與 `quadratic-formula-completing-square.html` 串成一致的國三到高一代數路徑。
4. `quadratic-functions-learning.html`：二次函數圖形、頂點、零點與最值。

這四頁完成後，學生會有一條明確路徑：

`運算順序 → 未知數 → 一元一次方程式 → 不等式與數線 → 比例 → 一次函數 → 二次方程式 → 二次函數`

### 第三階段：幾何與資料素養補齊

1. `similar-triangles-learning.html`：相似、比例與實測。
2. `junior-data-literacy.html`：統計圖表、代表值、抽樣與誤導資料。
3. 根式運算頁：同類根式、根式化簡與分母有根號。
4. 將三視圖、尺規作圖、四邊形翻牌補成有隨機題與錯誤回饋的完整練習頁。

### 第四階段：高中從概念頁補到解題力

1. 微積分四個短題組：極限、微分、微分應用、積分／面積。
2. 正弦定理、餘弦定理與解三角形。
3. 以新加入的向量線性組合頁為起點，補向量加減、倍數與座標表示的短自測，銜接既有內積與空間向量。
4. 多項式、最值、指數對數與面積公式頁補「選方法 → 寫一步 → 完整解析」題組。

---

## 7. 後續新增或大改頁面時的最低完成條件

每次做一個正式教學頁，建議在製作前後都用以下清單檢查：

### 教學內容

- [ ] 說清楚本頁的先備知識與學習目標。
- [ ] 先用情境、數線、表格、圖形或操作建立直覺，再給規則。
- [ ] 至少有 1 題完整例題：判斷 → 列式 → 計算 → 檢查 → 結論。
- [ ] 至少點出 2 個學生常犯錯誤，並說明錯在哪裡。
- [ ] 有基礎、進階、挑戰三層練習，且答案不是只顯示對錯。
- [ ] 能說明這頁學完後應接到哪一頁。

### 網頁與驗收

- [ ] 在首頁有正確卡片、章節代碼與用途標籤。
- [ ] 章節目錄、首頁卡片、實際檔案三者一致。
- [ ] 可從頁首回首頁；學生也能找到下一步。
- [ ] 內嵌 JavaScript 語法與主要控制流程通過檢查。
- [ ] 使用本機 HTTP，而非 `file://`，檢查頁面可開啟。
- [ ] 在實際桌機與手機寬度中檢查公式、SVG、Canvas、按鈕與橫向溢位。
- [ ] 若更動快取或共用資源，連同版本與快取策略一起驗收。

---

## 8. 最適合立即開始的下一件事

若只選一個「內容」專案，建議立刻做 **一元一次方程式互動教學頁**；它能補上目前最明顯的國中代數斷點，也能直接連到既有的不等式、比例、未知數、運算金字塔與直線工具。

若先選一個「系統」專案，建議先做 **首頁卡片／章節目錄一致性與用途標籤**。這會使現有 51 個可發現頁面先變得更容易備課、上課與自學，也能避免之後頁面愈多、分類愈難維護。
